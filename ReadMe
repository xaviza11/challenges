This project is maded for improve my code skills, it contais some challenges. They are finded on a websides, for the moment I'm using edabit.com, but in the future also can add oder pages. In each cases, the links at included inside a console.log so you can go to the page. It works with NodeJS so for run it just execute --node index.js-- and they run all the tests. The test are inside of the logic file and also are maded whit node.